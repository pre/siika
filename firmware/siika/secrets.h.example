// WiFi credentials for the NTP time sync (see plans/detection-and-idle.md).
// Copy this file to secrets.h and fill in the real values. secrets.h is
// gitignored and never committed; this .example file is the committed template.
#pragma once

#define WIFI_SSID "your-wifi-name"
#define WIFI_PASS "your-wifi-password"
